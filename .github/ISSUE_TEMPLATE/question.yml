name: Question
description: Ask a question (check the Help Centre first).
labels: ["question", "needs-triage"]
body:
  - type: textarea
    id: question
    attributes:
      label: Your question
    validations: { required: true }
