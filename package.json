{
  "name": "circls-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Circls platform monorepo — Fastify API + Next.js portals.",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm --filter @circls/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
